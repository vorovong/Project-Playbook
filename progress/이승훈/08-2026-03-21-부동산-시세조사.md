# 부동산 시세조사 — 진행 기록

## 기본 정보

- **작성자**: 이승훈
- **시작일**: 2026-03-21
- **한줄 목표**: 네이버 부동산 데이터를 자동 수집하여 상가 시세(매매/전세/월세) 엑셀 보고서를 생성
- **상태**: 완료

---

## 2026-04-06

### 대화 흐름

| 내가 한 말 | 결과 |
|---|---|
| 자동수집 시작해보자 | 급매물 수집기 실행 → 429 차단으로 전 구 수집 실패 |
| 계속 안되는데 이게 맞는거야? | 연속 실행 5~6회 누적으로 네이버 IP 장기 차단 확인 |
| 이 아이피를 풀 수 있는 방법이 있어? | 공유기 재부팅(IP 변경), 휴대폰 핫스팟, VPN 3가지 방법 안내 |

### 결과

- **딜레이 강화:** API 호출 간격 8→15초, 랜덤 범위 0~3초→2~5초
- **재시도 로직 개선:** 429 재시도 3→5회, 대기 60→120초 점진 증가
- **구별 재시도 추가:** 429 스킵 시 180초 대기 후 1회 재시도 (기존: 60초 대기 후 바로 스킵)
- **인코딩 에러 수정:** ⚠ 이모지 → [!] 텍스트 (cp949 환경 호환)

### 어려웠던 것 / 다음에 할 것

- 연속 실행 누적으로 네이버 IP 장기 차단 — 공유기 재부팅으로 IP 변경 후 재실행 필요
- 다음: 공유기 재부팅 후 수집 테스트, 정상 동작 확인

---

## 2026-04-02

### 대화 흐름

| 내가 한 말 | 결과 |
|---|---|
| 자동실행되고 텔레그램으로 안 왔어 | 작업 스케줄러 진단: bat 아닌 python 직접 실행 + WorkingDirectory 미설정 → bat 실행으로 수정 |
| 48시간? 매일 문제 생기는 거 아냐? | 429는 개발 테스트 누적 때문. 근본 대책 2가지 제안 |
| 1번 맞아, 서울만 해줘 | 조회 범위 서울만으로 축소 + 429/오류 시 텔레그램 알림 추가 |

### 결과

- **작업 스케줄러 수정:** python.exe 직접 실행 → cmd.exe로 bat 파일 실행 (WorkingDirectory 설정 포함)
- **조회 범위 축소:** 서울/경기/인천 66개 → 서울 25구만 (API 호출 1/3 감소)
- **429 알림 추가:** 차단돼도 텔레그램으로 "수집 실패 + 스킵된 지역" 알림
- **오류 알림 추가:** 예상치 못한 오류 발생 시에도 텔레그램 알림
- **텔레그램 발송 테스트 완료** — 정상 수신 확인

### 어려웠던 것 / 다음에 할 것

- 네이버 API 429 차단 아직 안 풀림 — 실제 수집 테스트는 차단 해제 후
- 다음: 텔레그램 토큰 @BotFather에서 revoke + .env 교체
- 다음: 429 해제 후 실행 테스트 → 정상 동작 확인

---

## 2026-04-01

### 대화 흐름

| 내가 한 말 | 결과 |
|---|---|
| 어제 할일 미리 생각하지않았나? | progress에서 다음 할 것 3가지 확인 |
| 1번(스케줄러 확인)부터 | 시작 프로그램 바로가기가 한글 경로 문제로 실행 실패 확인 → 작업 스케줄러로 변경 |
| 2번(첫 실행 전부 신규 문제) | 첫 실행은 기준선 수집 처리, 2회차부터 신규 분류 |
| 3번(토큰 revoke) | @BotFather에서 직접 처리 필요 (미완) |

### 결과

- **자동실행 방식 변경:** 시작 프로그램 바로가기 → Windows 작업 스케줄러 등록 (한글 경로 문제 해결)
- **429 무한루프 방지:** 2회 재시도 후 시군구 스킵 + 1분 휴식, 스킵된 지역 로그 표시
- **API 딜레이 증가:** 3초→5초 + 랜덤 0~3초 (예상 실행시간 약 8분)
- **첫 실행 기준선:** collected_ids.json이 비어있으면 기준선 수집으로 처리, 신규 리포트 미생성

### 어려웠던 것 / 다음에 할 것

- cmd.exe에서 한글 경로 bat 실행이 chcp 65001로도 안 되는 문제 → 작업 스케줄러로 우회
- 429 차단으로 실행 테스트 미완 — 차단 풀린 후 테스트 필요
- 다음: 텔레그램 토큰 @BotFather에서 revoke + .env 교체, 실행 테스트

---

## 2026-03-31

### 대화 흐름

| 내가 한 말 | 결과 |
|---|---|
| 급매물 수집기 크리틱해보자 | 보안/기능/구조 5가지 이슈 도출 |
| 하나씩 해보자 | 5개 항목 순차 개선 완료 |

### 결과

- **보안:** 텔레그램 토큰을 .env 파일로 분리 (v1, v2 모두)
- **매물 누락 방지:** 시도 단위 → 시군구 단위 조회로 변경 (서울 25구, 경기 31시군, 인천 10구군 개별 조회)
- **운영 안정성:** bat 실행 시 log.txt에 시작/종료 시각, 출력, 에러 기록
- **정리:** v1 급매물_수집기.py 삭제
- **투자 판단 지표:** 평단가(만원/평) 컬럼 + 요약 시트 평균 평단가 추가

### 배운 개발 지식

- 📐 시크릿 매니지먼트 (Secret Management) 🔒
  텔레그램 토큰이 소스코드에 하드코딩되어 git에 노출됨.
  선택지: ①그냥 둔다(편하지만 위험) ②.env 파일로 분리(한 단계 더 필요하지만 안전)
  판단: git 히스토리에 한 번 올라가면 영구적으로 남음. 분리 필수.
  = 비밀번호를 포스트잇에 붙여놓으면 안 되는 것과 같다.

- 📐 쿼리 그래뉼래리티 (Query Granularity) ⚖️
  API에 서울 전체를 한 번에 요청 vs 구 단위로 25번 요청.
  선택지: ①한 번에(빠르지만 페이징 한계로 누락) ②쪼개서(느리지만 정확)
  판단: 매물 누락은 수집기의 존재 이유를 훼손함. 정확성 우선.
  = 그물 눈이 너무 크면 작은 물고기를 놓치는 것과 같다.

### 어려웠던 것 / 다음에 할 것

- API 429(요청 제한)로 실행 테스트가 제한됨 — 시간 지나면 자동 해제
- git 히스토리에 남은 토큰은 텔레그램 @BotFather에서 재발급(revoke) 권장
- 다음: 작업 스케줄러 등록 확인, 첫 실행 시 전부 "신규"로 뜨는 문제 개선

---

## 2026-03-21

### 대화 흐름

| 내가 한 말 | 결과 |
|---|---|
| 부동산 시세 조사를 자동화하고 싶어 | 네이버 부동산 API 활용한 Python 프로그램 설계 |
| GUI로 쉽게 쓸 수 있게 해줘 | Tkinter GUI 모드 추가 (지역명 + 거래유형 선택 후 조회) |
| 링크도 복사할 수 있게 해줘 | 하이퍼링크 + URL 텍스트(복사용) 컬럼 분리 추가 |

### 결과

- **시세조사.py** 완성 (Python, 20KB):
  - GUI 모드: 지역명 입력 + 거래유형 체크박스 → 조회 버튼
  - CLI 모드: 명령줄로 직접 실행 가능
  - 네이버 부동산 API → 클러스터 조회 → 개별 매물 상세 수집
  - OpenStreetMap 역지오코딩 (위도/경도 → 한글 주소)
  - 상가/사무실/건물 필터링, m²→평 자동 변환
  - 거래유형별 시트 분리 (매매/전세/월세)
  - 평단가 평균, 월세 평균 등 통계 자동 계산
  - 요약 시트 생성
- **부동산_시세조사.spec** — PyInstaller 설정 (exe 패키징용)

### 배운 개발 지식

- **API 크롤링 파이프라인** — 지도 API → 클러스터 조회 → 개별 매물 상세 조회라는 다단계 구조. 한 번에 모든 데이터를 가져올 수 없어서 단계별로 파고 들어가는 방식.
- **역지오코딩 (Reverse Geocoding)** — 위도/경도 좌표를 사람이 읽을 수 있는 주소로 바꾸는 것. OpenStreetMap의 무료 API 활용.
- **Tkinter GUI** — Python에 기본 내장된 화면 만들기 도구. 별도 설치 없이 바로 사용 가능.
- **PyInstaller** — Python 스크립트를 exe 파일로 만들어서 Python 없이도 실행 가능하게 하는 도구. 다른 사람에게 프로그램을 배포할 때 사용.

- 📐 데이터 수집 안정성 (Data Collection Stability) ⚖️
  네이버 부동산 API에서 데이터를 가져올 때 요청 속도 제한.
  선택지: ①빠르게 한번에(차단 위험) ②1초 딜레이(안정적이지만 느림)
  판단: 차단되면 아예 못 쓰니까 안정성 우선.
  = 급하게 달리다 넘어지면 더 느린 것. 천천히 확실하게.

### 어려웠던 것 / 다음에 할 것

- exe 패키징으로 팀원 배포 가능
- 다음: 조회 지역 범위 확대, 데이터 축적 후 시세 변동 추적

---
