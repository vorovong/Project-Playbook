# 월정산 자동화 청사진 (v2 — 모듈화)

> 공존공간에서 구축한 월 운영현황 자동화 시스템을 다른 업체에 적용하기 위한 청사진

---

## 1. 이 시스템이 하는 일

**매월 반복되는 운영현황 정산을 자동으로 만들어준다.**

구글시트에서 수동으로 하던 월 운영현황 작성을:
1. 데이터를 자동으로 수집하고 (POS, 메일, 웹)
2. 수동 입력이 필요한 건 노션 DB에 직접 입력하고
3. 스크립트 한 줄로 월 정산 보고서를 노션에 자동 생성

```
node monthly-report.js gongzon 2026-03
→ 노션에 "26.03월 운영현황" 페이지 + 서브페이지 자동 생성
```

---

## 2. 모듈화 구조

### 전체 파일 구조

```
프로젝트/
├── monthly-report.js          ← 오케스트레이터 (90줄, 업체 공통)
├── lib/                       ← 공통 라이브러리 (어떤 업체든 그대로)
│   ├── notion.js              ← 노션 API 헬퍼
│   ├── blocks.js              ← 노션 블록 빌더 (표, 제목, 구분선)
│   └── weeks.js               ← 주차 기반 월 범위 계산
├── businesses/                ← 업체별 폴더
│   └── gongzon/               ← 공존공간
│       ├── config.js          ← 설정값 (인건비, 고정비, 수수료, DB ID)
│       ├── collectors.js      ← 데이터 수집 함수 5개
│       └── sheets.js          ← 시트 빌더 함수 7개
└── .env                       ← 노션 API 키, DB ID 등
```

### 각 파일의 역할

| 파일 | 역할 | 새 업체 시 |
|---|---|---|
| `monthly-report.js` | 업체 로드 → 데이터 수집 → 페이지 생성 | 수정 안 함 |
| `lib/notion.js` | notionPost, queryAll, createSubPage | 수정 안 함 |
| `lib/blocks.js` | heading, text, table, row, divider, fmt | 수정 안 함 |
| `lib/weeks.js` | getMonthRange (월~일 4주 계산) | 수정 안 함 |
| `businesses/XX/config.js` | 인건비, 고정비, 수수료율, DB ID, 부모페이지 | **복사 후 값 수정** |
| `businesses/XX/collectors.js` | 데이터 수집 함수 목록 | **POS/메일 다르면 수정** |
| `businesses/XX/sheets.js` | 시트 빌더 함수 목록 | **시트 구성 다르면 수정** |

---

## 3. 새 업체 적용 시 파악할 것 (체크리스트)

### A. 사업 구조

| 질문 | 예시 (공존공간) | 새 업체 |
|---|---|---|
| 사업 영역이 뭔가? | 부동산(임대), 양조장, 워크샵 | |
| 임차인/직영 구분은? | 미식가(임차), 포토박스(임차), 재생전술(직영) | |
| 수수료 구조는? | 카드 15%, 현금 13% (미식가) / 운영수수료 15% (포토) | |
| 고정 인건비는? | 박승현 500만, 엄수빈 245만, 박주진 5만 | |
| 고정비 항목은? | 대출이자, 정수기, 통신, 캡스, 기장료, 승강기 | |
| 임대료 수익은? | 763만원/월 | |

### B. 정산 주기

| 질문 | 예시 (공존공간) | 새 업체 |
|---|---|---|
| 정산 단위? | 주간 (월~일) | |
| 월은 몇 주? | 4주 고정 | |
| 이월 규칙? | 마지막 주 이후 남은 날 → 다음 달 | |
| 공과금 정산율? | 2026-03까지 95%, 이후 100% | |

### C. 데이터 소스

| 데이터 | 수집 방법 | 예시 (공존공간) | 새 업체 |
|---|---|---|---|
| 매출 | POS 자동수집 | OKPOS (Puppeteer) | |
| 공과금 | 메일 자동수집 | 네이버+Gmail (IMAP) | |
| 수도요금 | 웹 자동수집 | 수원시 수도요금 조회 | |
| 부스/매장 매출 | 수동 입력 | 노션 DB | |
| 구매내역 | 수동 입력 | 노션 DB | |
| 클래스/이벤트 | 수동 입력 | 노션 DB | |
| 근태 | 차후 자동화 | 세콤 (미구현) | |

### D. 시트(서브페이지) 구성

| 시트 | 공존공간 구성 | 새 업체 |
|---|---|---|
| 월 결산 | 매출 vs 지출 요약 + 부문별 상세 + 고정비 | |
| 임차인 매출 상세 | 일별·주차별 매출 + 수수료 계산 | |
| 공과금 | 정산율 적용 + 고지서 대조 | |
| 임차인별 매출 | 부스별/항목별 매출 + 재고 | |
| 인건비 | 소속별 인건비 | |
| 구매내역 | 온라인/오프라인 + 파트별 합산 | |
| 이벤트/클래스 | 유형별 매출·비용·이익 | |

---

## 4. 시스템 구성도

```
┌─────────── 자동 수집 ───────────┐     ┌──── 수동 입력 ────┐
│                                  │     │                    │
│  POS(매출)  메일(공과금)  웹(수도)│     │  노션 DB에 직접    │
│     ↓           ↓          ↓     │     │  입력              │
│  okpos-    utility-    water-    │     │                    │
│  sales.js  autofill.js bill.js  │     │  포토인더박스 DB    │
│     ↓           ↓          ↓     │     │  구매내역 DB       │
└─────┬───────────┬──────────┬─────┘     │  재생전술 DB       │
      │           │          │           └────────┬───────────┘
      └───────────┼──────────┘                    │
                  ↓                               │
         ┌───────────────┐                        │
         │   노션 DB     │←───────────────────────┘
         └───────┬───────┘
                 │
                 ↓
    ┌─────────────────────────────────────┐
    │  node monthly-report.js gongzon     │
    │                    2026-03          │
    │                                     │
    │  1. businesses/gongzon/config.js    │ ← 설정값 로드
    │  2. businesses/gongzon/collectors.js│ ← 데이터 수집
    │  3. businesses/gongzon/sheets.js    │ ← 시트 생성
    │  4. lib/notion.js                   │ ← 노션 API
    └───────────────┬─────────────────────┘
                    │
                    ↓
    ┌───────────────────────────────┐
    │ 노션: 월정산 페이지            │
    │                               │
    │  26.03월 운영현황 (월 결산)    │
    │    ├ 미식가의주방 총매출       │
    │    ├ 미식가의주방 공과금       │
    │    ├ 포토인더박스              │
    │    ├ 인건비                    │
    │    ├ 구매내역                  │
    │    └ 재생전술                  │
    └───────────────────────────────┘
```

---

## 5. 새 업체 적용 순서 (5단계)

### 1단계: 사업 구조 파악 (1시간)
- 체크리스트 A~D 채우기
- 기존 정산 파일(엑셀/시트) 분석
- 데이터 소스 확인 (POS 종류, 메일 형식 등)

### 2단계: 업체 폴더 생성 (30분)
```bash
cp -r businesses/gongzon businesses/새업체
```
- `config.js` 수정: 인건비, 고정비, 수수료율, 임대료
- `.env`에 새 노션 DB ID 추가
- 노션 DB 생성 (`create-operation-dbs.js` 복사 → 필드 수정 → 실행)

### 3단계: collectors.js 수정 (1~3시간)
- POS가 같으면 → 수집 함수 그대로
- POS가 다르면 → 새 수집 함수 작성 (가장 시간 걸리는 부분)
- 공과금 메일 형식 다르면 → 파서 수정
- 수동 입력만이면 → 이 단계 생략 가능

### 4단계: sheets.js 수정 (30분~1시간)
- 시트 구성이 비슷하면 → 제목/라벨만 수정
- 사업 영역이 다르면 → 빌더 함수 추가/삭제/수정

### 5단계: 테스트 + 운영 (1시간)
```bash
node monthly-report.js 새업체 2026-04
```
- 1개월치 데이터 입력 후 실행
- 기존 엑셀/시트와 숫자 대조
- 매월: 수동 입력 DB 채우기 → 스크립트 실행

---

## 6. 코드 분류: 수정 안 함 vs 복사 후 수정 vs 새로 작성

### 수정 안 함 (공통 모듈)

| 파일 | 역할 |
|---|---|
| `monthly-report.js` | 오케스트레이터 (업체 로드 → 수집 → 생성) |
| `lib/notion.js` | 노션 API 헬퍼 (인증, 쿼리, 페이지 생성) |
| `lib/blocks.js` | 블록 빌더 (표, 제목, 구분선) |
| `lib/weeks.js` | 주차 계산 (월~일 4주) |

### 복사 후 수정 (업체별 폴더)

| 파일 | 수정 내용 |
|---|---|
| `config.js` | 인건비, 고정비, 수수료율, DB ID — **값만 바꾸기** |
| `collectors.js` | DB 필드명, 수수료 계산식 — **POS 같으면 거의 안 바꿈** |
| `sheets.js` | 시트 제목, 테이블 구성 — **사업 구조 같으면 거의 안 바꿈** |

### 새로 작성 (업체마다 다른 것)

| 항목 | 이유 |
|---|---|
| POS 수집 스크립트 | POS 시스템마다 로그인/데이터 구조가 다름 |
| 공과금 메일 파서 | 메일 형식이 업체/지역마다 다름 |
| 수도요금 크롤러 | 지자체 웹사이트가 다름 |

---

## 7. 예상 소요 시간

| 단계 | 시간 | 비고 |
|---|---|---|
| 사업 구조 파악 | 1시간 | 체크리스트 기반 |
| 업체 폴더 생성 + config | 30분 | 복사 후 값 수정 |
| collectors 수정 | 1~3시간 | POS 같으면 30분, 다르면 3시간 |
| sheets 수정 | 30분~1시간 | 사업 구조 비슷하면 30분 |
| 테스트 + 검증 | 1시간 | |
| **합계** | **4~6시간** | 공존공간은 ~15시간 (모듈화 전) |

**모듈화 전 청사진만 있을 때: 5~7시간**
**모듈화 후: 4~6시간 (사업 구조 비슷하면 3시간)**

---

## 8. 기술 스택

| 구성요소 | 기술 | 역할 |
|---|---|---|
| 런타임 | Node.js | 스크립트 실행 |
| 백엔드 DB | 노션 API | 데이터 저장/조회 |
| 로컬 DB | SQLite (better-sqlite3) | PDF/프로젝트 관리 |
| 웹 크롤링 | Puppeteer | POS/공과금/순위 수집 |
| 메일 수집 | IMAP + mailparser | 공과금 메일 파싱 |
| 알림 | Telegram Bot | 정산 완료 알림 |
| 웹 UI | Express + HTML | 대시보드 (선택) |

### 필수 패키지
```bash
npm install dotenv puppeteer better-sqlite3 express imap mailparser
```

---

*이 청사진은 공존공간 월정산 자동화 구축 경험(2026-03~04)을 기반으로 작성됨*
*v2: 모듈화 완료 (2026-04-03) — businesses/ 폴더 구조로 다중 업체 지원*
